@extends('layouts.app')

@section('content')
    <div class="container-fluid">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Confirmation {{ $confirmation->order->name }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/confirmation') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/confirmation/' . $confirmation->id . '/edit') }}" title="Edit user"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/confirmation', $confirmation->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete user',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $confirmation->id }}</td>
                                    </tr>
                                    <tr><th> Name Orderer</th><td> {{ $confirmation->order->name }} </td></tr>
                                    <tr><th> Menu order </th><td>{{ isset($confirmation->menu->name) ? $confirmation->menu->name : "This order don't have menu" }}</td></tr>
                                    <tr><th> Dishes in Menu </th><td>{{ isset($confirmation->menu->dishes) ? implode(', ',$confirmation->menu->dishes->pluck('name')->toArray()) : "This order don't have any dishes" }}</td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
